Friend Module InteractAction
    Friend Function Run(model As IWorldModel) As Boolean
        'interactable character names
        'if only one, go straight to interacting
        'otherwise, choose which character to interact with
        Throw New NotImplementedException()
    End Function
End Module
